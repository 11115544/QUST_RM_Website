# 数据库连接（二选一）

# 方式一：SQLite（推荐新手，无需安装数据库，直接运行即可）
DATABASE_URL="file:./dev.db"

# 方式二：PostgreSQL（正式部署时使用，需先安装 PostgreSQL）
# DATABASE_URL="postgresql://用户名:密码@localhost:5432/数据库名"

# 网站地址（本地开发保持下面即可）
NEXTAUTH_URL="http://localhost:3000"

# 登录加密密钥（必填）。可随意写一串长字符串，例如：my-secret-key-12345-change-in-production
NEXTAUTH_SECRET="请改成任意一串随机字符"
